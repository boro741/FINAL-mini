<link rel="stylesheet" href="/css/bootstrap.css" />
<style>
  #postId{
      width: 800px;
      height: 300px
  }

  ul {
      list-style: none;
  }
  .page-header{
      margin: 0%;
  }

</style>
<script
  src="https://code.jquery.com/jquery-3.3.1.min.js"
  integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8="
  crossorigin="anonymous"></script>
 <h2 class="page-header">Home</h2>
<h4>Upcoming Events:</h4> 
<ul class="post"></ul>

<script>
    $(document).ready(function(){
        	var eventName = '{{eventName}}';
	        var description = '{{description}}';
	        var poster = '{{poster}}';
	        var eventDate = '{{eventDate}}';
	        var mobileNo = '{{mobileNo}}';
	        var emailId = '{{emailId}}';
	        var price = '{{price}}';

        posts = poster.split(',');
        posts.forEach(function(url){
           //console.log("test");
           if(!url) return;
           // This is for local 
            //var posterURL = window.location.href + url;

            // Heroku URL:
            var posterURL = 'https://s3.ap-south-1.amazonaws.com/student-chapt/' + url;
            console.log('posterURL:: ',posterURL);
            $("<br><br><li><a href='"+posterURL+"'> <img id='postId' src='"+ posterURL +"'></a><li />").appendTo('ul.post');
        }); 
        console.log('posts: ',eventName);
    });
    
</script>
